@model IEnumerable<ToeicCentre_Management.Models.Cauhoibaitap>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.IdPhieuBaiTapNavigation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MaChNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IdPhieuBaiTapNavigation.IdPhieuBaiTap)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MaChNavigation.MaCh)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdBaiTap">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdBaiTap">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdBaiTap">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
